import React from 'react';
import ReactDOM from 'react-dom';
import { Provider } from 'react-redux';

document.addEventListener("DOMContentLoaded", () => {
  ReactDOM.render(
    <h1>I AM WORKING</h1> , document.getElementById('content')
    );
});
